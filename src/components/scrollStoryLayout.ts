/**
 * Document-space height the ScrollStory section always reserves.
 *
 * It lives in its own module so the lazy boundary can reserve the exact same
 * space without pulling GSAP and the three scenes into the entry chunk.
 */
export const SCROLL_STORY_HEIGHT = '550vh';
